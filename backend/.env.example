# MongoDB Connection String
# Required for production. Leave empty for local development (uses in-memory DB).
MONGO_URL=

# JWT Secret for authentication
# REQUIRED for production. Leave empty for local dev (uses fallback).
JWT_SECRET=

# Server Port (defaults to 5000)
PORT=5000

# CORS Allowed Origins (comma-separated)
CORS_ORIGIN=http://localhost:5173